import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button } from './';

<Meta title="Docs/Components/Button" component={Button} />

# Button

With `MDX`, we can define a story for `Button` right in the middle of our
Markdown documentation.

<!--- This is your Story template function, shown here in React -->

<Canvas>
  <Button label="Primary" size="small" />
  <Button label="Primary" />
  <Button label="Primary" size="large" />
  <Button label="Secondary" primary={false} size="small" />
  <Button label="Secondary" primary={false}/>
  <Button label="Secondary" primary={false} size="large" />
</Canvas>
